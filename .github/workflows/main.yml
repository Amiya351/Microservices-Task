name: CI/CD Build and Push Docker Images
on:
  push:
    branches:
      - main
env:
  REGISTRY: docker.io
  IMAGE_TAG: ${{ github.sha }}
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service:
          - user-service
          - product-service
          - order-service
          - gateway-service
    steps: 
